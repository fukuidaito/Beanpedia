<%= search_form_for q, url: url do |f| %>
    <div class="input-group mb-3">
      <%= f.search_field :title_or_body_cont, placeholder: t('defaults.search_word') %><%= f.submit  class: 'btn' %>
    </div>
  <% end %>