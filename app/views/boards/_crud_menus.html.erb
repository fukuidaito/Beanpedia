<ul class='crud-menu-btn list-inline float-right'>
  <li class="list-inline-item">
  <%= link_to edit_board_path(board), id: "button-edit-#{board.id}" do %>
      <%= icon 'fa', 'pen' %>
    <% end %>
  </li>
  <li class="list-inline-item">
  <%= link_to board_path(board), id: "button-delete-#{board.id}", data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } do %>
  <%= icon('fas', 'trash') %>
<% end %>
  </li>
</ul>
