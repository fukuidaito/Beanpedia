<% provide(:title, "QUEST一覧") %>
<%= render 'search_form', q: @q, url: boards_path %>
  <% if @boards.present? %>
  <div class="row mb-2">
    <%= render @boards %>
  </div>
  <% else %>
    <p>掲示板がありません</p>
  <% end %>
  </div> 